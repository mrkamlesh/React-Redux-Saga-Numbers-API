import React from 'react'

export default function Form(props) {
  return (
    <div>
      <input type="text" placeholder="Please provide a number or type random" />
      <button className="btn btn-primary">Click to get info</button>
    </div>
  )
}
